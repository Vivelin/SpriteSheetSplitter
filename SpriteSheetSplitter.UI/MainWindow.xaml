<Window x:Class="SpriteSheetSplitter.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
		xmlns:Controls="clr-namespace:SpriteSheetSplitter.UI.Controls"
        Title="MainWindow" Height="350" Width="525"
		TextOptions.TextFormattingMode="Display"
		Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
	<DockPanel>
		<StackPanel HorizontalAlignment="Right" Orientation="Horizontal" 
					DockPanel.Dock="Bottom" Style="{DynamicResource PanelBase}">
			<Button>Save...</Button>
		</StackPanel>
		<StackPanel Width="150" DockPanel.Dock="Right" 
					Style="{DynamicResource PanelBase}">
			<Label>Delay (ms):</Label>
			<xctk:IntegerUpDown Minimum="0" />

			<Label>Transparent color:</Label>
			<xctk:ColorPicker DisplayColorAndName="True"
							  UsingAlphaChannel="False" />

			<Label>Scale factor:</Label>
			<xctk:SingleUpDown Minimum="0" />
		</StackPanel>
		<Grid Style="{DynamicResource GridBase}">
			<Controls:SpriteSheetControl />
		</Grid>
	</DockPanel>
</Window>
